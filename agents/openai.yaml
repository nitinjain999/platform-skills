# generated by platform-skills
interface:
  display_name: "Platform Skills"
  short_description: "Platform engineering workflows with rollback plans"
  default_prompt: "Use $platform-skills to review this platform change for ownership, blast radius, validation, and rollback."

policy:
  allow_implicit_invocation: true

agents:
  coordinator:
    description: Lead orchestrator. Routes tasks, owns AGENTS.md. Source of truth is AGENTS.md.
    model: claude-sonnet-4-6
  skill-author:
    description: Authors new skills, commands, reference files. Owns skills/, references/, examples/, commands/.
    model: claude-sonnet-4-6
  release:
    description: Runs release checklist — version sync, SKILL.md consistency, Status labels, marketplace.json source.sha. Proposes only, never commits.
    model: claude-sonnet-4-6
  reviewer:
    description: Pre-merge reviewer. Checks stale paths, missing Status labels, version consistency. Read-only.
    model: claude-haiku-4-5-20251001
  navigator:
    description: Read-only contributor guide. Explains repo structure, where files go, how CI works.
    model: claude-haiku-4-5-20251001
