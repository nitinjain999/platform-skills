apiVersion: v1
kind: Pod
metadata:
  name: app-pod
  namespace: production
spec:
  containers:
    - name: app
      image: registry.internal.example.com/app:2.0.0
      securityContext:
        privileged: false
        runAsNonRoot: true
        readOnlyRootFilesystem: true
