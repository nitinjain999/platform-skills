# Task: Locate credential and permission configuration in GitHub Actions

Scan the workflow files in `workflows/` and report all credential and permission configuration:

For each workflow, report:
- OIDC or token authentication configuration
- Repository permissions (read/write scopes)
- Secrets referenced
- External credential providers (AWS, Azure, GCP)

State what was omitted or truncated. Report complete, partial, or blocked.
