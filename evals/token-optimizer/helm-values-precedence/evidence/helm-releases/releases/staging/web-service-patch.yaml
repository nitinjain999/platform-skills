apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: web-service
  namespace: apps
spec:
  values:
    image:
      tag: "v1.5.1-rc.2"
    replicaCount: 2
    resources:
      requests:
        memory: 128Mi
